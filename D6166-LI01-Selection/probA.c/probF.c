#include <stdio.h>

int max;
long long d, s, t;

int main (){

    scanf("%lld %lld %lld", &d, &s, &t); getchar();


    int max = d;
    if (max > s){
        
        int max = s;
    } else if (max > t){

        int max = t;
    }

    
    
    
    

    return 0;
}