#include <stdio.h>

char x1, x2, x3;
int x, y, z;

int main (){

    scanf("%c %c %c", &x1, &x2, &x3); getchar();
    
    x = x1;
    y = x2;
    z = x3;

    if (x < y < z){
   
        
    }
    
    

    return 0;
}