#include <stdio.h>

int N;
char M[40], P[40];
int S;

int main(){

    // FILE *fp = ("testdata.txt", "w");

    // scanf("%d", &N);
    // for (int i = 0; i < N; i++){

    //     fprintf(fp, "%[^#]#%[^\n]", &M, &P);
    //     getchar();
    // }
    
    // fclose(fp);

    scanf("%d", &S);
    for (int i = 0; i < S; i++){
       
       
    }
    


    return 0;
}